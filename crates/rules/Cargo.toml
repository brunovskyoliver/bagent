[package]
name = "bagent-rules"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_yaml = "0.9"
serde_json = "1"
regex = "1"
tracing = "0.1"
anyhow = "1"
tokio = { version = "1", features = ["sync", "fs", "time", "rt"] }
