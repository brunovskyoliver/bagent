# Follow-up: fullscreen notch visibility

The fullscreen-notch visibility script fails identically at the Stage 8 baseline (`68fdd2b`) and final Stage 8 integration revision. This is outside evidence-default-enablement scope. No UI behavior is changed by this task.

Acceptance: make the fullscreen status surface visible without changing evidence routing or canonical evidence behavior.
