# Follow-up: non-notch inline pill

The non-notch inline-pill script fails identically at the Stage 8 baseline (`68fdd2b`) and final Stage 8 integration revision. This is outside evidence-default-enablement scope. No UI behavior is changed by this task.

Acceptance: restore the expected non-notch inline presentation independently of evidence routing.
