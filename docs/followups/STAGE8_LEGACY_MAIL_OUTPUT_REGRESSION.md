# Follow-up: legacy Mail-output regression script

The legacy Mail-output regression script fails identically at the Stage 8 baseline (`68fdd2b`) and final Stage 8 integration revision. This is outside evidence-default-enablement scope. The evidence route remains opt-in and the legacy route is not changed by this task.

Acceptance: repair or replace the regression script against the legacy Mail output contract without enabling evidence routing.
